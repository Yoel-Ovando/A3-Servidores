# A3-Servidores
# pasos para iniciar servidor

1. Actualizar repositorios 'apt-get update'
2. Instalar Servidor ssh 'apt-get install ssh-server'
3. Modificar configuraci√n en / etc/ssh/sshd_config
4. Iniciar servidor 'Service ssh star'
